# unsupported or invalid adapters fail before invoking source callbacks

    Code
      dl_validate(product)
    Condition
      Error in `abort()`:
      ! This component needs a dl_check_component() preflight method.

