% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lakefold-package.R
\docType{package}
\name{lakefold-package}
\alias{lakefold}
\alias{lakefold-package}
\title{lakefold: checked, versioned data for recurring reports}
\description{
Keep incoming files, check proposed tables and publish data that reports can
read consistently. Corrections create new releases while earlier versions
remain available. Start with \code{\link[=dl_product]{dl_product()}}, \code{\link[=dl_add_source]{dl_add_source()}} and \code{\link[=dl_run]{dl_run()}}.
Native products need no database. Add \code{\link[=dl_publish]{dl_publish()}} for durable lake storage.
Add business checks, reusable tables and recorded metrics when needed.
}
\section{Why use lakefold?}{

A corrected monthly delivery should update the current dataset without losing
the inputs behind a report already issued. A failed delivery should leave the
previous successful release available. lakefold records these steps and lets
you read either the current table or an exact historical release.
Publishing means making checked data readable in your lake, not on the internet.
See \code{vignette("why-lakefold")} for an illustrated explanation and
\code{vignette("getting-started")} for a complete monthly reporting example.
}

\section{Design principles}{

Define, inspect, execute and examine results through a small set of consistent
concepts. Sources, contracts, pipelines, products, metrics and releases have
explicit responsibilities. Integrations can be adopted as the workflow needs
them. See \code{vignette("design-review")} for composition and extension boundaries.
}

\section{Start here}{

\itemize{
\item \code{vignette("composing-products")}: simple workflows and the mental model.
\item \code{vignette("extending-lakefold")}: implement S3 component adapters.
\item \code{vignette("why-lakefold")}: purpose, benefits and concepts in plain language.
\item \code{vignette("getting-started")}: deliveries, corrections, checks and reports, step by step.
\item \code{vignette("dbt-workflows")}: dbt setup, builds, diagnostics and dm models.
\item \code{vignette("workflow-design")}: specifications and the execution lifecycle.
\item \code{vignette("quality-history")}: quality gates and reproducible releases.
\item \code{vignette("quality-gates")}: pointblank, input gates and quality reports.
\item \code{vignette("products-metrics")}: products, metrics and report manifests.
}
}

\section{Execution paths}{

\code{\link[=dl_run]{dl_run()}} executes composed products in memory or through their target.
\code{\link[=dl_ingest]{dl_ingest()}} and lake targets publish immutable lakefold releases after
contract validation. \code{\link[=dl_dbt_build]{dl_dbt_build()}} runs dbt models and tests, which have
dbt's own materialization semantics. A successful dbt build does not create
a lakefold release automatically. \code{\link[=dl_model]{dl_model()}} opens governed releases;
\code{\link[=dl_dbt_model]{dl_dbt_model()}} opens current dbt relations.
\code{\link[=dl_dbt_publish]{dl_dbt_publish()}} snapshots one current dbt relation, validates its contract
and publishes it as a governed release. \code{\link[=dl_status]{dl_status()}} and \code{\link[=dl_quality]{dl_quality()}}
provide common inspection functions across both execution paths.
}

\section{Resource ownership}{

Core configuration is connection-free. DBI source and lake target adapters
may retain caller-owned connections; factories defer opening. Close connections
with \code{\link[=dl_close]{dl_close()}} or \code{\link[=dl_disconnect]{dl_disconnect()}}. Close local catalog connections before invoking dbt
in another process. Lazy tables require their originating connection to
remain open. The lakefold registry requires a single writer.
}

\seealso{
\code{\link[=dl_config]{dl_config()}}, \code{\link[=dl_pipeline]{dl_pipeline()}}, \code{\link[=dl_dbt_project]{dl_dbt_project()}}
}
\author{
\strong{Maintainer}: Jan-Hendrik Weinert \email{65009403+JanWein@users.noreply.github.com}

Authors:
\itemize{
  \item Jan-Hendrik Weinert \email{65009403+JanWein@users.noreply.github.com}
}

}
\keyword{internal}
