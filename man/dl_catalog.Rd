% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/catalog.R
\name{dl_catalog}
\alias{dl_catalog}
\title{Start a read-only data catalog}
\usage{
dl_catalog(
  lake = NULL,
  snapshot = NULL,
  launch = interactive(),
  refresh_seconds = 30
)
}
\arguments{
\item{lake}{Connected lake, or NULL when reading an exported snapshot.}

\item{snapshot}{Path to JSON exported by dl_catalog_export().}

\item{launch}{Start the app; FALSE returns a shiny.appobj for Connect.}

\item{refresh_seconds}{Metadata refresh interval.}
}
\value{
A Shiny app object (when launch = FALSE).
}
\description{
Start a read-only data catalog
}
\examples{
root <- tempfile("lakefold-example-")
config <- dl_config(
  dl_catalog_duckdb(file.path(root, "lake.db")),
  dl_storage_local(file.path(root, "data")),
  landing = file.path(root, "landing"), backend = "duckdb"
)
lake <- dl_connect(config)
app <- dl_catalog(lake, launch = FALSE)
class(app)
dl_disconnect(lake)
unlink(root, recursive = TRUE)
}
