% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ingest.R
\name{dl_ingest}
\alias{dl_ingest}
\title{Land, validate and publish one source file}
\usage{
dl_ingest(
  lake,
  source,
  contract,
  asset,
  version = "1.0.0",
  code_version,
  layer = "validated",
  input_contract = NULL,
  ...
)
}
\arguments{
\item{lake}{A connected lake or a connection-free \code{\link[=dl_config]{dl_config()}}.}

\item{source}{A \code{\link[=dl_source]{dl_source()}} definition with a local file and reader.}

\item{contract}{A \code{\link[=dl_contract]{dl_contract()}} defining the publication gate.}

\item{asset}{Character scalar giving the governed output asset ID.}

\item{version}{Character scalar giving the ingestion definition version.}

\item{code_version}{Character scalar identifying all ingestion code and
dependencies. Change both versions when behavior changes.}

\item{layer}{Character scalar giving the publication schema, \code{"validated"}
by default. Raw extraction always uses the configured \code{"raw"} schema.}

\item{input_contract}{Optional separate contract for an input gate before
writing Raw. Requires a reader returning a data frame. The final candidate
is still validated against \code{contract}.}

\item{...}{Arguments passed to \code{\link[=dl_run]{dl_run()}}, such as \code{business_date} or
\code{stop_on_failure}. Do not pass another \code{lake} argument.}
}
\value{
A \code{dl_run_result} with \code{run_id}, \code{status}, \code{release_id} and
\code{quality}.
Connections opened from a config are closed before returning.
}
\description{
A compact entry point for the standard ingestion pipeline. It performs the
same immutable landing and contract gate as explicit pipeline steps. Use
\code{\link[=dl_pipeline]{dl_pipeline()}} when transformations or partition replacement are needed.
}
\examples{
root <- tempfile("lakefold-example-")
config <- dl_config(
  dl_catalog_duckdb(file.path(root, "lake.db")),
  dl_storage_local(file.path(root, "data")),
  landing = file.path(root, "landing"), backend = "duckdb"
)
lake <- dl_connect(config)
path <- file.path(root, "orders.csv")
utils::write.csv(data.frame(order_id = 1:2, amount = c(25, 75)), path,
  row.names = FALSE)
source <- dl_source("orders.file", path, reader = utils::read.csv)
contract <- dl_contract(
  "orders", "1.0.0", "Analytics", "Order amounts", "One order",
  c(order_id = "integer", amount = "numeric"), key = "order_id"
)
release <- dl_ingest(lake, source, contract, "orders", code_version = "v1")
release
dl_disconnect(lake)
unlink(root, recursive = TRUE)
}
\seealso{
\code{\link[=dl_pipeline]{dl_pipeline()}}, \code{\link[=dl_tbl]{dl_tbl()}}, \code{\link[=dl_execute]{dl_execute()}}
}
