% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dbt.R
\name{dbt_build}
\alias{dbt_build}
\alias{dbt_test}
\title{Build or test SQL models with dbt}
\usage{
dbt_build(
  project,
  select = NULL,
  exclude = NULL,
  full_refresh = FALSE,
  vars = list(),
  echo = TRUE,
  timeout = Inf,
  stop_on_failure = TRUE
)

dbt_test(
  project,
  select = NULL,
  exclude = NULL,
  vars = list(),
  echo = TRUE,
  timeout = Inf,
  stop_on_failure = TRUE
)
}
\arguments{
\item{project}{A \code{\link[=dbt_project]{dbt_project()}} specification.}

\item{select, exclude}{Optional character vectors of dbt selection
expressions.
Each element is one CLI argument; expressions starting with \code{-} are
rejected.}

\item{full_refresh}{Logical scalar. Rebuild incremental models when \code{TRUE}.}

\item{vars}{A named list of non-secret dbt variables, encoded as JSON.}

\item{echo}{Logical scalar. Stream dbt output to the R console.}

\item{timeout}{Positive timeout in seconds, or \code{Inf} for no limit.}

\item{stop_on_failure}{Logical scalar. Raise \code{tw_dbt_failed} on a nonzero
exit
status, failed nodes or missing artifacts. The condition's \code{result} field
retains diagnostics. Set \code{FALSE} to inspect failures as ordinary results.}
}
\value{
A \code{tw_dbt_result} list with \code{status} (integer exit code), \code{success}
(logical), \code{command}, \code{results} (node tibble), parsed \code{manifest},
\code{artifacts_dir}, \code{stdout}, \code{stderr} and \code{artifact_error}. Warnings reported
by dbt are retained and do not by themselves count as failure.
}
\description{
Run the dbt CLI in a separate process and read that invocation's artifacts.
Each invocation gets a new directory under \code{.tidyweave/runs} in the project;
a failed invocation can never reuse an earlier run's successful results.
}
\details{
Close R connections to the same local DuckDB or DuckLake catalog before
running dbt, then reconnect for analysis. dbt manages its own connections.
Anonymous usage telemetry is disabled for the child process.
A dbt build is not an atomic tidyweave release: earlier models can have been
materialized even if a later model or test fails. dbt logs and artifacts can
contain SQL, paths and database messages; treat them as project data.
}
\examples{
\dontshow{if (nzchar(Sys.getenv("TIDYWEAVE_DBT_EXAMPLE_PROJECT"))) withAutoprint(\{ # examplesIf}
project <- dbt_project(Sys.getenv("TIDYWEAVE_DBT_EXAMPLE_PROJECT"))
result <- dbt_build(project, select = "tag:reporting", echo = FALSE)
dbt_status(result)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=dbt_status]{dbt_status()}}, \code{\link[=dbt_lineage]{dbt_lineage()}}, \code{\link[=dbt_model]{dbt_model()}}
}
