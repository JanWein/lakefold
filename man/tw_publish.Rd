% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dbt-publish.R, R/execution.R
\name{tw_publish.tw_dbt_result}
\alias{tw_publish.tw_dbt_result}
\alias{tw_publish}
\alias{tw_publish.data.frame}
\alias{tw_publish.tw_product}
\title{Execute and publish a product with sensible local defaults}
\usage{
\method{tw_publish}{tw_dbt_result}(x, name = NULL, to = NULL, execution = NULL, ...)

tw_publish(x, name = NULL, to = NULL, ...)

\method{tw_publish}{data.frame}(x, name = NULL, to = NULL, ...)

\method{tw_publish}{tw_product}(x, name = NULL, to = NULL, layer = NULL, execution = NULL, ...)
}
\arguments{
\item{x}{Composed product, data frame, or successful dbt build.}

\item{name}{Product name for a data frame, or an exact or unambiguous model
name for a dbt build. Omit it for an already named product.}

\item{to}{Optional target, replacing an already configured target. Defaults
to the \code{tidyweave} folder in the working directory when the product has none.
For dbt builds it defaults to the project's configured lake.}

\item{execution}{Optional \code{\link[=tw_execution_config]{tw_execution_config()}} defaults for products,
overriding defaults stored by \code{tw_product(execution = )}.}

\item{...}{Execution options, including \code{data} or \code{sources} for new deliveries
as described in \code{\link[=tw_run]{tw_run()}}, \code{stop_on_failure} and, for lake
targets, \code{business_date}, \code{notify}, \code{cache} and \code{previous}. Supply a previous
publication to reject stale corrections if the destination has changed. dbt builds accept
\code{\link[=tw_dbt_publish]{tw_dbt_publish()}} options such as \code{contract}, \code{asset} and \code{layer}.}

\item{layer}{Optional publication layer for a lake target.}
}
\value{
A run result. An exception on failure includes \code{condition$result}.
}
\description{
Use \code{\link[=tw_trial]{tw_trial()}} to try a product without configured framework writers.
\code{tw_run()} executes its full configuration, including targets and catalogs.
\code{tw_publish()} adds a local lake target when none was supplied. A new folder
uses DuckDB; an existing folder retains its saved backend. \code{\link[=tw_target_lake]{tw_target_lake()}}
accepts other lake configurations. Execution automatically validates the
definition before source acquisition.
Use \code{\link[=tw_collect]{tw_collect()}} to obtain the output as an ordinary tibble.
}
\section{Publishing a dbt model}{

Use \code{tw_dbt_project(path, lake = config) |> tw_run() |> tw_publish("model")} to
create an immutable, checked lake release. The destination is inferred from
the managed project's configuration. A model name must identify exactly one
materialized node that succeeded in this build. Artifact hashes and parsed
objects are checked again before publication. Pass \code{contract}, \code{asset},
\code{layer} or other \code{\link[=tw_dbt_publish]{tw_dbt_publish()}} options through \code{...}.

Publication snapshots the current relation and checks that complete copy.
Build provenance does not prove a mutable relation is unchanged since dbt
finished. Coordinate writers between build and publication.
}

\examples{
\dontshow{if (requireNamespace("duckdb", quietly = TRUE)) (if (getRversion() >= "3.4") withAutoprint else force)(\{ # examplesIf}
root <- tempfile("tidyweave-")
result <- tw_product("orders") |>
  tw_add_source(data.frame(id = 1:2)) |>
  tw_publish(to = root)
tw_collect(result)
unlink(root, recursive = TRUE)
\dontshow{\}) # examplesIf}
}
