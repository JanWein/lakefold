% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline.R
\name{dl_pipeline}
\alias{dl_pipeline}
\alias{dl_step_land}
\alias{dl_step_extract}
\alias{dl_step_validate}
\alias{dl_step_publish}
\title{Build a pipeline specification}
\usage{
dl_pipeline(id, lake = NULL, version = "1.0.0", code_version, config = NULL)

dl_step_land(pipeline, source)

dl_step_extract(pipeline, using = NULL, into = "raw")

dl_step_validate(pipeline, contract)

dl_step_publish(
  pipeline,
  into,
  mode = c("replace", "replace_partition"),
  partition_by = character(),
  layer = "validated"
)
}
\arguments{
\item{id}{Pipeline identifier.}

\item{lake}{A connection-free dl_config or a connected lake; only
configuration is retained.}

\item{version}{Definition version.}

\item{code_version}{Version of all execution code, e.g. a Git commit SHA.
Change it when imported functions, dependencies or captured values change.}

\item{config}{Optional named alternative to lake for a connection-free
dl_config.}

\item{pipeline}{Pipeline specification.}

\item{source}{Source definition.}

\item{using}{Reader override.}

\item{into}{Target layer for extraction, or asset id for publication.}

\item{contract}{Contract definition.}

\item{mode}{Full replacement or replacement of partitions present in input.}

\item{partition_by}{Partition columns. Empty or NULL keys are rejected.}

\item{layer}{Publication schema.}
}
\value{
A pipeline specification, with no open connection or loaded data.
}
\description{
Build a pipeline specification
}
\examples{
contract <- dl_contract(
  "orders", "1.0.0", "Analytics", "Order amounts", "One order",
  c(order_id = "integer", amount = "numeric"), key = "order_id"
)
pipeline <- dl_pipeline("orders.import", dl_config(backend = "duckdb"),
  code_version = "v1") |>
  dl_step_land(dl_source("orders.file", "orders.csv", utils::read.csv)) |>
  dl_step_extract() |>
  dl_step_validate(contract) |>
  dl_step_publish("orders")
dl_plan(pipeline)
}
