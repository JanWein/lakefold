% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/components.R
\name{sql_transform}
\alias{sql_transform}
\title{Apply a DuckDB SQL query to a data frame}
\usage{
sql_transform(query)
}
\arguments{
\item{query}{SQL query referencing the input table \code{data}.}
}
\value{
A transformation accepted by \code{\link[=add_transform]{add_transform()}}.
}
\description{
The input is available as \code{data} in a private, temporary DuckDB connection.
SQL is executed unchanged by DuckDB and must return a table. This adapter is
intended for trusted analytical queries; it does not sandbox SQL.
}
\examples{
\dontshow{if (requireNamespace("duckdb", quietly = TRUE)) withAutoprint(\{ # examplesIf}
product("totals") |>
  add_source(data.frame(amount = c(10, 20))) |>
  add_transform(sql_transform("SELECT sum(amount) AS total FROM data")) |>
  run() |>
  collect()
\dontshow{\}) # examplesIf}
}
