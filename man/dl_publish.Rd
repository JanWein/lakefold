% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/execution.R
\name{dl_publish}
\alias{dl_publish}
\title{Execute and publish a product with sensible local defaults}
\usage{
dl_publish(x, name = NULL, to = NULL, ...)
}
\arguments{
\item{x}{Composed product, or a data frame for immediate publication.}

\item{name}{Product name; required for a data-frame expression.}

\item{to}{Optional target, replacing an already configured target. Defaults
to the \code{lakefold} folder in the working directory when the product has none.}

\item{...}{Execution options, including \code{stop_on_failure} and, for lake
targets, \code{business_date}, \code{notify} and \code{cache}.}
}
\value{
A run result. An exception on failure includes \code{condition$result}.
}
\description{
\code{dl_run()} executes a composed product in memory unless it has a target.
\code{dl_publish()} adds a local lake target when none was supplied. A folder
string uses DuckDB; \code{\link[=dl_target_lake]{dl_target_lake()}} accepts other lake configurations.
Both automatically validate the definition before source acquisition.
Use \code{\link[=dl_collect]{dl_collect()}} to obtain the output as an ordinary tibble.
}
\examples{
\dontshow{if (requireNamespace("duckdb", quietly = TRUE)) withAutoprint(\{ # examplesIf}
root <- tempfile("lakefold-")
result <- dl_product("orders") |>
  dl_add_source(data.frame(id = 1:2)) |>
  dl_publish(to = root)
dl_collect(result)
unlink(root, recursive = TRUE)
\dontshow{\}) # examplesIf}
}
