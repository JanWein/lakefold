% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dbt.R
\name{tw_dbt_project}
\alias{tw_dbt_project}
\title{Describe a dbt project for execution from R}
\usage{
tw_dbt_project(
  path = "dbt",
  profiles_dir = NULL,
  target = NULL,
  executable = "dbt",
  lake = NULL,
  sources = NULL
)
}
\arguments{
\item{path}{Character scalar giving the directory with \code{dbt_project.yml}.
The directory is checked when executing, not when constructing the object.}

\item{profiles_dir}{Character scalar pointing to a directory containing
\code{profiles.yml}, or \code{NULL} to use dbt's own profile discovery.}

\item{target}{Optional character scalar naming a target in the dbt profile.}

\item{executable}{Character scalar giving a dbt executable name or path.
It is passed to \code{\link[processx:run]{processx::run()}} without a shell.}

\item{lake}{Optional connection-free \code{\link[=tw_lake_config]{tw_lake_config()}} for local DuckDB or
DuckLake. At execution, tidyweave writes a private profile for this catalog.
Omit \code{profiles_dir} when using \code{lake}. SQL and handwritten project files
stay unchanged. Close any caller-owned connections before running dbt.}

\item{sources}{Optional named list of successful published lake results.
They become the \code{inputs} source group. Use \code{\link[=tw_dbt_sources]{tw_dbt_sources()}} to add groups
from other physical schemas. Definitions retain exact release IDs;
registry resolution and YAML generation happen only at execution.}
}
\value{
A serializable \code{dbt_project} specification. It contains no
database connection or resolved environment credentials.
}
\description{
Store paths and CLI settings without running dbt or opening a database.
dbt remains responsible for SQL models, dependencies, tests and incremental
strategies. Use \code{\link[=tw_dbt_build]{tw_dbt_build()}} or \code{\link[=tw_run]{tw_run()}} to execute the project.
}
\examples{
project <- tw_dbt_project("analytics", profiles_dir = "analytics")
project
}
\seealso{
\code{\link[=tw_dbt_init]{tw_dbt_init()}}, \code{\link[=tw_dbt_status]{tw_dbt_status()}}, \code{\link[=tw_dbt_model]{tw_dbt_model()}}
}
