% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dbt.R
\name{dl_dbt_status}
\alias{dl_dbt_status}
\title{Inspect the node results of a dbt invocation}
\usage{
dl_dbt_status(x)
}
\arguments{
\item{x}{A \code{dl_dbt_result}, or a character scalar giving an artifact
directory.}
}
\value{
A tibble with character columns \code{unique_id}, \code{status}, \code{message},
numeric \code{execution_time} (seconds) and integer \code{failures} (possibly \code{NA}).
}
\description{
Read structured statuses without parsing console output. A directory must
contain a matching pair of \code{run_results.json} and \code{manifest.json} artifacts.
The returned table contains executed nodes only, not the entire dbt project.
}
\examples{
artifacts <- system.file("extdata", "dbt-artifacts", package = "lakefold")
dl_dbt_status(artifacts)
}
\seealso{
\code{\link[=dl_dbt_build]{dl_dbt_build()}}, \code{\link[=dl_dbt_lineage]{dl_dbt_lineage()}}
}
