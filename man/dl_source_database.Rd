% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/components.R
\name{dl_source_database}
\alias{dl_source_database}
\title{Describe a DBI table or SQL query as a source}
\usage{
dl_source_database(connection, table = NULL, query = NULL, params = NULL)
}
\arguments{
\item{connection}{DBI connection or function opening one.}

\item{table}{Table name or \code{\link[DBI:Id]{DBI::Id()}}.}

\item{query}{A single SQL query string.}

\item{params}{Parameters passed to \code{\link[DBI:dbGetQuery]{DBI::dbGetQuery()}}.}
}
\value{
A source specification accepted by \code{\link[=dl_add_source]{dl_add_source()}}.
}
\description{
Supply exactly one of \code{table} and \code{query}. The connection may be an open DBI
connection or a zero-argument factory. Only factory-created connections are
closed by lakefold. SQL is passed to DBI unchanged; use \code{params} for values.
Nothing is queried during construction or structural preflight.
}
\examples{
\dontshow{if (requireNamespace("duckdb", quietly = TRUE)) withAutoprint(\{ # examplesIf}
con <- DBI::dbConnect(duckdb::duckdb())
DBI::dbWriteTable(con, "orders", data.frame(id = 1:2))
dl_read_source(dl_source_database(con, table = "orders"))
DBI::dbDisconnect(con, shutdown = TRUE)
\dontshow{\}) # examplesIf}
}
