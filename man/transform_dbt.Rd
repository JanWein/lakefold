% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transform-dbt.R
\name{transform_dbt}
\alias{transform_dbt}
\title{Transform staged product input with a dbt model}
\usage{
transform_dbt(
  project,
  model,
  connection,
  input,
  full_refresh = FALSE,
  vars = list(),
  echo = FALSE,
  timeout = Inf
)
}
\arguments{
\item{project}{A \code{\link[=dbt_project]{dbt_project()}} specification.}

\item{model}{Exact materialized dbt model unique ID, for example
\code{"model.shop.customer_revenue"}.}

\item{connection}{Zero-argument function opening a DBI connection to the
database used by dbt. Return a new connection on every call.}

\item{input}{Dedicated staging table, as a string or \code{\link[DBI:Id]{DBI::Id()}}. Existing
table contents are replaced. Its schema must already exist.}

\item{full_refresh, vars, echo, timeout}{Passed to \code{\link[=dbt_build]{dbt_build()}}.}
}
\value{
A deferred transformation adapter for \code{\link[=add_transform]{add_transform()}}.
}
\description{
Writes the incoming table to an explicitly named staging relation, closes
that connection, runs \verb{dbt build} for the selected model, then opens a fresh
connection to read the successfully built relation. The dbt model must
reference the supplied staging table; this adapter never ignores its input.
}
\details{
\code{input} is replaced on every execution. Use a dedicated staging table that
is not managed by a dbt seed or model, and coordinate concurrent writers.
dbt owns SQL dependencies, tests and incremental strategies. Staging and dbt
model changes are external side effects; the product's final quality gate
controls its publication target, not a rollback of dbt's database changes.
The returned table is materialized so every factory-owned connection closes.
Invocation ID and selected model are attached as \code{tw_transform_metadata}.
}
\examples{
# dbt's selected model must read the dedicated staged_orders relation.
project <- dbt_project("analytics", profiles_dir = "analytics")
step <- transform_dbt(project, "model.shop.customer_revenue",
  connection = function() DBI::dbConnect(duckdb::duckdb(), "analytics.duckdb"),
  input = "staged_orders")
inspect(step)
}
\seealso{
\code{\link[=dbt_init]{dbt_init()}}, \code{\link[=dbt_build]{dbt_build()}}, \code{\link[=source_database]{source_database()}}
}
