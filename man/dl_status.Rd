% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{dl_status}
\alias{dl_status}
\title{Inspect execution status across R and dbt workflows}
\usage{
dl_status(x, asset = NULL)
}
\arguments{
\item{x}{A connected lake, \code{\link[=dl_run]{dl_run()}} result or \code{\link[=dl_dbt_build]{dl_dbt_build()}} result.}

\item{asset}{Optional asset ID when querying a lake.}
}
\value{
A tibble with \code{engine}, \code{id}, \code{status}, \code{success}, \code{release_id},
\code{asset} and \code{message}. A dbt process failure remains visible even when
individual nodes passed. No raw stdout or stderr is included.
}
\description{
Inspect execution status across R and dbt workflows
}
\examples{
root <- tempfile("lakefold-")
lake <- dl_connect(dl_config(dl_catalog_duckdb(file.path(root, "lake.db")),
  dl_storage_local(file.path(root, "data")),
  landing = file.path(root, "landing"), backend = "duckdb"))
dl_status(lake)
dl_disconnect(lake)
unlink(root, recursive = TRUE)
}
