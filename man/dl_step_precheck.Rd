% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/input-gate.R
\name{dl_step_precheck}
\alias{dl_step_precheck}
\title{Check a delivery before writing its raw table}
\usage{
dl_step_precheck(pipeline, contract)
}
\arguments{
\item{pipeline}{Pipeline after \code{\link[=dl_step_extract]{dl_step_extract()}}.}

\item{contract}{Contract for the extracted input, possibly containing
\code{\link[=dl_pointblank]{dl_pointblank()}} rules. It can differ from the final product contract.}
}
\value{
The updated pipeline specification; no IO is performed.
}
\description{
Add after extraction and before transformations. The source file is retained
in immutable landing even when the input gate blocks. The reader must return
a data frame, ensuring the checked values are the values subsequently written.
The mandatory candidate gate remains in place, including after partition
composition. A failed input gate is persisted with stage \code{"ingest"}.
}
\examples{
contract <- dl_contract("orders", "1", "Analytics", "Orders", "One order",
  c(id = "integer"), key = "id")
pipeline <- dl_pipeline("orders.import", dl_config(backend = "duckdb"),
  code_version = "v1") |>
  dl_step_land(dl_source("orders.file", "orders.csv", utils::read.csv)) |>
  dl_step_extract() |>
  dl_step_precheck(contract) |>
  dl_step_validate(contract) |>
  dl_step_publish("orders")
dl_plan(pipeline)
}
\seealso{
\code{\link[=dl_ingest]{dl_ingest()}}, \code{\link[=dl_validate]{dl_validate()}}
}
