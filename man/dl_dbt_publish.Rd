% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dbt-publish.R
\name{dl_dbt_publish}
\alias{dl_dbt_publish}
\title{Snapshot and publish one dbt relation as a governed release}
\usage{
dl_dbt_publish(
  lake,
  result,
  model,
  contract,
  asset,
  code_version,
  version = "1.0.0",
  layer = "products",
  business_date = NA_character_,
  notify = NULL,
  stop_on_failure = TRUE
)
}
\arguments{
\item{lake}{Connected lake with the dbt relation attached.}

\item{result}{Successful result from \code{\link[=dl_dbt_build]{dl_dbt_build()}}.}

\item{model}{Exact dbt unique ID, such as \code{"model.shop.customer_revenue"}.}

\item{contract}{Contract for the copied candidate.}

\item{asset}{Governed asset ID.}

\item{code_version}{Version of publication code and all relevant
dependencies.}

\item{version}{Publication definition version.}

\item{layer}{Configured destination layer.}

\item{business_date}{Business date, separate from build or publication time.}

\item{notify}{Optional existing notification callback.}

\item{stop_on_failure}{Signal failure after persisting quality evidence.}
}
\value{
A \code{dl_run_result}. Use \code{\link[=dl_tbl]{dl_tbl()}} with its release ID for immutable
consumption, and \code{\link[=dl_model]{dl_model()}} for relationships between pinned releases.
}
\description{
Requires a successful \verb{dbt build} result containing the selected materialized
node. Copies the relation currently visible to this connection into an
immutable candidate, validates the supplied contract, then commits the
release
marker. Later dbt builds cannot change this snapshot. The dbt invocation is
recorded as provenance, not as proof that the live relation has not changed
since that build. Coordinate writers between build and publication.
}
\details{
Each call validates a fresh snapshot; there is no cache based solely on a
dbt invocation ID. This prevents changed tables reusing stale quality
evidence.
This publishes one relation, not an atomic bundle of all dbt models.
}
\examples{
# After dl_dbt_build() and reopening the lake connection:
# release <- dl_dbt_publish(lake, result, "model.shop.customer_revenue",
#   contract, "shop.revenue", code_version = "v1")
# dl_tbl(lake, "shop.revenue", release$release_id)
}
