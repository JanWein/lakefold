% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/composition.R
\name{dl_add_source}
\alias{dl_add_source}
\alias{dl_add_transform}
\alias{dl_add_contract}
\alias{dl_add_quality}
\alias{dl_add_target}
\alias{dl_add_catalog}
\title{Compose a data product using ordinary R objects}
\usage{
dl_add_source(x, source, reader = NULL)

dl_add_transform(x, transform, name = NULL)

dl_add_contract(x, contract)

dl_add_quality(x, quality, name = NULL)

dl_add_target(x, target)

dl_add_catalog(x, catalog)
}
\arguments{
\item{x}{Product created with \code{dl_product("name")}.}

\item{source}{Data frame, file path, function or source adapter.}

\item{reader}{Optional file reader. CSV, TSV, RDS and Excel have defaults.}

\item{transform}{R function, formula using \code{.x}, or transform adapter.}

\item{name}{Optional step name, generated when omitted.}

\item{contract}{Contract, named type vector, or named list of prototypes.}

\item{quality}{One-sided row predicate, function, rule, or list of rules.
Formula \code{NA} results count as failures. Functions return scalar logicals
or \code{\link[=dl_quality_counts]{dl_quality_counts()}}. Names in rule lists become rule names.}

\item{target}{Lake folder path, connected lake, configuration or target
adapter. Use \code{\link[=dl_target_lake]{dl_target_lake()}} for partition or layer options.}

\item{catalog}{Function receiving run metadata, or catalog adapter.}
}
\value{
An updated product specification. No source data are read.
}
\description{
These functions describe work without executing it. Start with
\code{dl_product("orders")}, add a data frame, file path, zero-argument function
or source adapter, then add only the capabilities you need.
Transformations run in addition order; the contract and quality checks run
on the final candidate. Adding a source, contract or target replaces that
component. Transforms, quality rules and catalogs accumulate.
}
\examples{
orders <- dl_product("orders") |>
  dl_add_source(data.frame(id = 1:2, amount = c(25, 75))) |>
  dl_add_transform(function(data) transform(data, amount = amount * 2)) |>
  dl_add_contract(c(id = "integer", amount = "numeric")) |>
  dl_add_quality(~ amount >= 0)
orders |> dl_run() |> dl_collect()
}
\seealso{
\code{\link[=dl_run]{dl_run()}}, \code{\link[=dl_publish]{dl_publish()}}, \code{\link[=dl_validate]{dl_validate()}}, \code{\link[=dl_inspect]{dl_inspect()}}
}
