% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/setup.R
\name{setup_lake}
\alias{setup_lake}
\alias{connect_lake}
\title{Set up or reconnect to a data lake}
\usage{
setup_lake(
  catalog = registry_duckdb("metadata.ducklake"),
  storage = storage_local("data"),
  layers = c("raw", "validated", "products"),
  landing = "landing",
  backend = c("ducklake", "duckdb"),
  install_extensions = TRUE,
  read_only = FALSE
)

connect_lake(config, read_only = config$read_only \%||\% FALSE)
}
\arguments{
\item{catalog}{Metadata configuration.}

\item{storage}{Data file storage configuration.}

\item{layers}{Schema names.}

\item{landing}{Local immutable landing directory (also used as staging for
S3).}

\item{backend}{DuckLake, or local DuckDB for offline development.}

\item{install_extensions}{Allow DuckDB to install required extensions.}

\item{read_only}{Attach existing storage read-only and skip schema creation
and migration. A lake created by a newer package may require an upgrade.}

\item{config}{A configuration from a previously connected lake.}
}
\value{
A connected lake handle. Close it with disconnect_lake().
}
\description{
Set up or reconnect to a data lake
}
\examples{
\dontshow{if (requireNamespace("duckdb", quietly = TRUE)) withAutoprint(\{ # examplesIf}
root <- tempfile("tidyweave-example-")
config <- lake_config(
  registry_duckdb(file.path(root, "lake.db")),
  storage_local(file.path(root, "data")),
  landing = file.path(root, "landing"), backend = "duckdb"
)
lake <- connect_lake(config)
lake
disconnect_lake(lake)
unlink(root, recursive = TRUE)
\dontshow{\}) # examplesIf}
}
