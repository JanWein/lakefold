% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/recipe.R
\name{tw_step_lookup}
\alias{tw_step_lookup}
\title{Add a checked lookup to a recipe}
\usage{
tw_step_lookup(
  x,
  source,
  by,
  engine = c("native", "dm"),
  unmatched = c("error", "keep"),
  suffix = c(".x", ".y"),
  name = NULL,
  table = NULL
)
}
\arguments{
\item{x}{A \code{\link[=tw_recipe]{tw_recipe()}} specification.}

\item{source}{Reference data, a path, source adapter, product, or successful
result. Lake results pin exact immutable releases; other results reuse
their retained output.}

\item{by}{Equality keys, supplied as a character vector, a named vector
mapping input to reference columns, or \code{\link[dplyr:join_by]{dplyr::join_by()}}. Inequality,
rolling and cross joins belong in ordinary dplyr transformations.}

\item{engine}{Constraint validation engine: \code{"native"} or optional \code{"dm"}.}

\item{unmatched}{Whether unmatched input rows cause an error or are retained
with missing reference values. Unused reference rows are always allowed.}

\item{suffix}{Two suffixes for overlapping non-key column names, as in
\code{\link[dplyr:mutate-joins]{dplyr::left_join()}}.}

\item{name}{Stable delivery name used by \code{sources = list(name = new_data)}.
Defaults to the reference product id or a bare source variable's name.
For expressions such as file readers, supply a name explicitly; otherwise
an automatic lookup name is used. \code{\link[=tw_explain]{tw_explain()}} shows the available names.}

\item{table}{Table to select when source is a successful model result. The
table name is also the default delivery name; published members stay pinned.}
}
\value{
An updated recipe.
}
\description{
Uses the same dependency resolution and relationship checks as \code{\link[=tw_add_lookup]{tw_add_lookup()}}.
Reusable lookup specifications can instead be passed to \code{\link[=tw_step_transform]{tw_step_transform()}}.
}
\examples{
tw_recipe() |>
  tw_step_lookup(data.frame(id = 1:2, region = c("North", "South")),
    by = "id", name = "customers")
}
