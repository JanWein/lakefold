% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trial.R
\name{tw_trial}
\alias{tw_trial}
\title{Try a product without invoking configured writers}
\usage{
tw_trial(x, data = NULL, sources = NULL, stop_on_failure = FALSE)
}
\arguments{
\item{x}{Product or modular \code{\link[=tw_workflow]{tw_workflow()}} definition.}

\item{data, sources}{Replacement delivery or named sources, as in \code{\link[=tw_run]{tw_run()}}.}

\item{stop_on_failure}{Defaults to \code{FALSE}: a failed trial returns a result
for \code{\link[=tw_quality_report]{tw_quality_report()}} and \code{\link[=tw_quality_rows]{tw_quality_rows()}}. Use \code{TRUE} to raise an error.
\code{\link[=tw_run]{tw_run()}} and \code{\link[=tw_publish]{tw_publish()}} still raise errors on failure by default.}
}
\value{
An in-memory run result accepted by \code{\link[=tw_collect]{tw_collect()}} and \code{\link[=tw_measure]{tw_measure()}}.
}
\description{
Executes the product's sources, transformations and checks, with targets,
catalogs and durable run evidence disabled throughout its dependency graph.
The original definition is unchanged. Source and transformation callbacks are
ordinary user code: their own side effects cannot be prevented by the framework.
Read-only access to existing published inputs is still allowed.
}
\examples{
orders <- tw_product("orders", data.frame(amount = c(10, 20))) |>
  tw_set_target("reporting-lake")
tw_trial(orders) |> tw_collect()
}
