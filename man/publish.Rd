% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dbt-publish.R, R/execution.R
\name{publish.tw_dbt_result}
\alias{publish.tw_dbt_result}
\alias{publish}
\alias{publish.data.frame}
\alias{publish.tw_product}
\title{Execute and publish a product with sensible local defaults}
\usage{
\method{publish}{tw_dbt_result}(x, name = NULL, to = NULL, ...)

publish(x, name = NULL, to = NULL, ...)

\method{publish}{data.frame}(x, name = NULL, to = NULL, ...)

\method{publish}{tw_product}(x, name = NULL, to = NULL, layer = NULL, ...)
}
\arguments{
\item{x}{Composed product, data frame, or successful dbt build.}

\item{name}{Product name for a data frame, or an exact or unambiguous model
name for a dbt build. Omit it for an already named product.}

\item{to}{Optional target, replacing an already configured target. Defaults
to the \code{tidyweave} folder in the working directory when the product has none.
For dbt builds it defaults to the project's configured lake.}

\item{...}{Execution options, including \code{stop_on_failure} and, for lake
targets, \code{business_date}, \code{notify} and \code{cache}. dbt builds accept
\code{\link[=dbt_publish]{dbt_publish()}} options such as \code{contract}, \code{asset} and \code{layer}.}

\item{layer}{Optional publication layer for a lake target.}
}
\value{
A run result. An exception on failure includes \code{condition$result}.
}
\description{
\code{run()} executes a composed product in memory unless it has a target.
\code{publish()} adds a local lake target when none was supplied. A folder
string uses DuckDB; \code{\link[=target_lake]{target_lake()}} accepts other lake configurations.
Both automatically validate the definition before source acquisition.
Use \code{\link[=collect]{collect()}} to obtain the output as an ordinary tibble.
}
\section{Publishing a dbt model}{

Use \code{dbt_project(path, lake = config) |> run() |> publish("model")} to
create an immutable, checked lake release. The destination is inferred from
the managed project's configuration. A model name must identify exactly one
materialized node that succeeded in this build. Artifact hashes and parsed
objects are checked again before publication. Pass \code{contract}, \code{asset},
\code{layer} or other \code{\link[=dbt_publish]{dbt_publish()}} options through \code{...}.

Publication snapshots the current relation and checks that complete copy.
Build provenance does not prove a mutable relation is unchanged since dbt
finished. Coordinate writers between build and publication.
}

\examples{
\dontshow{if (requireNamespace("duckdb", quietly = TRUE)) withAutoprint(\{ # examplesIf}
root <- tempfile("tidyweave-")
result <- product("orders") |>
  add_source(data.frame(id = 1:2)) |>
  publish(to = root)
collect(result)
unlink(root, recursive = TRUE)
\dontshow{\}) # examplesIf}
}
