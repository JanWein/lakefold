% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/maintenance.R
\name{dl_cleanup}
\alias{dl_cleanup}
\title{Preview or remove expired unpublished candidate tables}
\usage{
dl_cleanup(lake, older_than_days = 30, dry_run = TRUE, at = Sys.time())
}
\arguments{
\item{lake}{Connected lake.}

\item{older_than_days}{Positive retention period for failed-run tables.}

\item{dry_run}{Return a plan without removing tables. Defaults to \code{TRUE}.}

\item{at}{POSIXct scalar used to evaluate retention.}
}
\value{
A tibble with schema, table, run, age and action. Executed drops are
enclosed in one catalog transaction and eligibility is checked again.
}
\description{
Considers only framework-named tables belonging to completed blocked or
errored runs. Published release tables, running jobs, landing files, quality
evidence and report manifests are always retained. Execute only with one
coordinated writer, as required for all registry mutations.
}
\details{
Dropping a DuckLake table does not immediately reclaim physical object-store
files. This function does not expire DuckLake snapshots or run file cleanup.
}
\examples{
root <- tempfile("lakefold-")
lake <- dl_connect(dl_config(dl_catalog_duckdb(file.path(root, "lake.db")),
  dl_storage_local(file.path(root, "data")),
  landing = file.path(root, "landing"), backend = "duckdb"))
dl_cleanup(lake)
dl_disconnect(lake)
unlink(root, recursive = TRUE)
}
