% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quality-reports.R
\name{dl_quality_report}
\alias{dl_quality_report}
\title{Export a portable quality report}
\usage{
dl_quality_report(
  x,
  path,
  format = c("html", "json"),
  title = "lakefold quality report",
  overwrite = FALSE
)
}
\arguments{
\item{x}{Quality tibble or a run/dbt result accepted by \code{\link[=dl_quality]{dl_quality()}}.}

\item{path}{Output file path.}

\item{format}{HTML or JSON.}

\item{title}{Human-readable report title.}

\item{overwrite}{Replace an existing file only when explicitly requested.}
}
\value{
The normalized output path, invisibly.
}
\description{
Exports persisted check metadata, counts and segment labels. No source rows
are collected. The HTML is self-contained, escapes cell contents and displays
status as text. An empty result is explicitly reported as not checked.
}
\examples{
contract <- dl_contract("orders", "1", "Analytics", "Orders", "One order",
  c(id = "integer"), key = "id")
quality <- dl_validate(data.frame(id = c(1L, 1L)), contract)
path <- tempfile(fileext = ".html")
dl_quality_report(quality, path)
unlink(path)
}
\seealso{
\code{\link[=dl_pointblank_report]{dl_pointblank_report()}}, \code{\link[=dl_expect_quality]{dl_expect_quality()}}
}
