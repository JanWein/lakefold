% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/composition.R
\name{add_source}
\alias{add_source}
\alias{add_transform}
\alias{add_contract}
\alias{add_quality}
\alias{set_target}
\alias{add_catalog}
\title{Compose a data product using ordinary R objects}
\usage{
add_source(x, source, name = NULL, reader = NULL, replace = FALSE)

add_transform(x, transform, name = NULL)

add_contract(x, contract)

add_quality(x, quality, name = NULL)

set_target(x, target)

add_catalog(x, catalog, name = NULL)
}
\arguments{
\item{x}{Product created with \code{product("name")}.}

\item{source}{Data frame, file path, function or source adapter.}

\item{name}{Optional source, step or catalog name, generated when omitted.}

\item{reader}{Optional file reader. CSV, TSV, RDS and Excel have defaults.}

\item{replace}{Replace a source with the same name explicitly.}

\item{transform}{R function, formula using \code{.x}, or transform adapter.}

\item{contract}{Contract, named type vector, or named list of prototypes.}

\item{quality}{One-sided row predicate, function, rule, or list of rules.
Formula \code{NA} results count as failures. Functions return scalar logicals
or \code{\link[=quality_counts]{quality_counts()}}. Names in rule lists become rule names.}

\item{target}{Lake folder path, connected lake, configuration or target
adapter. Use \code{\link[=target_lake]{target_lake()}} for partition or layer options.}

\item{catalog}{Function receiving run metadata, or catalog adapter.}
}
\value{
An updated product specification. No source data are read.
}
\description{
These functions describe work without executing it. Start with
\code{product("orders")}, add a data frame, file path, zero-argument function
or source adapter, then add only the capabilities you need.
Transformations run in addition order; the contract and quality checks run
on the final candidate. Sources, transforms, rules and catalogs accumulate.
A contract or target replaces the previously configured component.
}
\examples{
orders <- product("orders") |>
  add_source(data.frame(id = 1:2, amount = c(25, 75))) |>
  add_transform(function(data) transform(data, amount = amount * 2)) |>
  add_contract(c(id = "integer", amount = "numeric")) |>
  add_quality(~ amount >= 0)
orders |> run() |> collect()
}
\seealso{
\code{\link[=run]{run()}}, \code{\link[=publish]{publish()}}, \code{\link[=validate]{validate()}}, \code{\link[=inspect]{inspect()}}
}
