% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dbt.R
\name{tw_dbt_model}
\alias{tw_dbt_model}
\title{Open materialized dbt relations as a lazy relational model}
\usage{
tw_dbt_model(
  lake,
  x,
  tables = NULL,
  database = "lake",
  primary_keys = list(),
  foreign_keys = list(),
  check = TRUE
)
}
\arguments{
\item{lake}{A connected lake from \code{\link[=tw_connect_lake]{tw_connect_lake()}}.}

\item{x}{A \code{tw_dbt_result}, or a character scalar giving an artifact
directory.}

\item{tables}{Optional named character vector mapping R table aliases to dbt
unique IDs, such as \code{c(sales = "model.shop.sales")}. By default includes
all
non-ephemeral models, seeds and snapshots. Use a subset after selected
builds.}

\item{database}{Character scalar: the attachment name in the R connection.
Defaults to \code{"lake"}, as created by \code{\link[=tw_connect_lake]{tw_connect_lake()}}.}

\item{primary_keys}{Named list of character vectors, keyed by table alias.}

\item{foreign_keys}{List of lists containing table, columns, ref_table,
ref_columns.}

\item{check}{Validate all declared keys and relationships.}
}
\value{
A lazy \code{dm} object with a \code{tw_dbt_nodes} attribute mapping aliases
to dbt unique IDs. Key violations raise \code{tw_model_invalid} when \code{check} is
\code{TRUE}; SQL and connection errors are propagated.
}
\description{
Build a \code{\link[dm:dm]{dm::dm()}} from explicit relation names in a dbt manifest. The lake
must refer to the same catalog used by the dbt project. SQL dependencies do
not imply relational keys; declare keys explicitly when needed.
}
\details{
Relations are current dbt tables or views, not immutable tidyweave releases.
The returned lazy model borrows the supplied connection; keep it open while
querying. Ephemeral models cannot be opened. This function does not certify
that tables were successfully built or that they still match the artifacts.
}
\examples{
\dontshow{if (nzchar(Sys.getenv("TIDYWEAVE_DBT_EXAMPLE_PROJECT"))) (if (getRversion() >= "3.4") withAutoprint else force)(\{ # examplesIf}
# See vignette("dbt-workflows") for a complete build and reconnect example.
project <- tw_dbt_project(Sys.getenv("TIDYWEAVE_DBT_EXAMPLE_PROJECT"))
project
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=tw_model]{tw_model()}} for immutable releases, \code{\link[=tw_dbt_lineage]{tw_dbt_lineage()}}
}
