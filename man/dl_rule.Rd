% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/contracts.R
\name{dl_rule}
\alias{dl_rule}
\alias{dl_quality_counts}
\alias{dl_pointblank}
\title{Define a quality rule}
\usage{
dl_rule(
  name,
  check,
  severity = c("error", "warning"),
  max_failure = 0,
  description = ""
)

dl_quality_counts(n_failed, n_total)

dl_pointblank(
  name,
  build,
  severity = c("error", "warning"),
  max_failure = 0,
  policy = c("rule", "agent")
)
}
\arguments{
\item{name}{Rule name.}

\item{check}{Function taking a table and returning a scalar logical or
dl_quality_counts(), or a one-sided row predicate such as \code{~ amount >= 0}.
Missing formula results count as failures. Lazy formulas run in the
database; arbitrary functions remain responsible for their own collection.}

\item{severity}{error blocks publication; warning permits publication.}

\item{max_failure}{Fraction of permitted failed test units.}

\item{description}{Rule description.}

\item{n_failed, n_total}{Failed and total test units.}

\item{build}{Function creating a pointblank agent from a lazy table.}

\item{policy}{\code{"rule"} preserves the explicit \code{severity} / \code{max_failure}
gate. \code{"agent"} uses pointblank's per-step action levels: warnings permit
publication, stop/error and critical states block. Native pointblank
threshold rounding applies. An unconfigured, inactive or errored agent
step always blocks. Old notify-only levels do not authorize publication.}
}
\value{
A rule specification or counts object.
}
\description{
Define a quality rule
}
\examples{
rule <- dl_rule("positive", function(data) {
  counts <- dplyr::summarise(data, failed = sum(amount <= 0), total = dplyr::n())
  if (inherits(counts, "tbl_sql")) counts <- dplyr::collect(counts)
  dl_quality_counts(counts$failed, counts$total)
})
rule$name
}
