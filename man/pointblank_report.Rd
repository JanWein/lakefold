% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quality-reports.R
\name{pointblank_report}
\alias{pointblank_report}
\title{Export the native pointblank report from a validation}
\usage{
pointblank_report(x, rule, path, overwrite = FALSE)
}
\arguments{
\item{x}{A quality tibble containing retained pointblank agents.}

\item{rule}{Name supplied to \code{\link[=pointblank_checks]{pointblank_checks()}}.}

\item{path}{Output HTML path.}

\item{overwrite}{Replace an existing output.}
}
\value{
The normalized output path, invisibly.
}
\description{
Requires \code{\link[=validate]{validate()}} with \code{keep_agents = TRUE}. Agents are held only in
memory and are never serialized into the registry. Failed-row extracts and
checked-table samples are disabled during interrogation. The native report
describes pointblank action levels; the authoritative tidyweave gate is shown
by \code{\link[=quality_report]{quality_report()}}, especially when using \code{policy = "rule"}.
}
\examples{
\dontshow{if (requireNamespace("pointblank", quietly = TRUE)) withAutoprint(\{ # examplesIf}
contract <- contract("orders", "1", "Analytics", "Orders", "One order",
  c(amount = "numeric"), rules = list(pointblank_checks("amounts", function(data) {
    pointblank::create_agent(data) |>
      pointblank::col_vals_gte("amount", 0)
  })))
quality <- validate(data.frame(amount = c(10, -1)), contract,
  keep_agents = TRUE)
path <- tempfile(fileext = ".html")
pointblank_report(quality, "amounts", path)
unlink(path)
\dontshow{\}) # examplesIf}
}
