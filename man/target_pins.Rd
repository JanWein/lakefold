% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adapter-pins.R
\name{target_pins}
\alias{target_pins}
\title{Publish a checked table to a pins board}
\usage{
target_pins(board, name, type = "rds", ...)
}
\arguments{
\item{board}{Configured pins board.}

\item{name}{Pin name.}

\item{type}{Storage format accepted by pins. Defaults to \code{"rds"}.}

\item{...}{Named arguments to \code{\link[pins:pin_write]{pins::pin_write()}}, such as \code{versioned = TRUE}.}
}
\value{
A target specification for \code{\link[=set_target]{set_target()}}.
}
\description{
Storage, authentication and version retention belong to pins and the board.
The output descriptor records the version reported by the board after writing
when available. Coordinate writers externally; this adapter does not promise
atomic or immutable publication. Extra arguments go to \code{\link[pins:pin_write]{pins::pin_write()}}.
}
\examples{
\dontshow{if (requireNamespace("pins", quietly = TRUE)) withAutoprint(\{ # examplesIf}
board <- pins::board_temp(versioned = TRUE)
product("orders") |>
  add_source(data.frame(id = 1:2)) |>
  set_target(target_pins(board, "orders")) |>
  run()
\dontshow{\}) # examplesIf}
}
