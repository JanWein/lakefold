% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simple.R
\name{dl_open}
\alias{dl_open}
\alias{dl_close}
\title{Open a local lake with sensible defaults}
\usage{
dl_open(path = "lakefold", backend = NULL, read_only = FALSE)

dl_close(lake)
}
\arguments{
\item{path}{Local folder, created if needed. Defaults to \code{"lakefold"} in
the working directory.}

\item{backend}{Optional \code{"duckdb"} or \code{"ducklake"}. DuckLake requires its
DuckDB extension. Defaults to the saved choice, or DuckDB for a new folder.}

\item{read_only}{Open an existing lake without registry or data writes.}

\item{lake}{Connected lake to close.}
}
\value{
\code{dl_open()} returns a connected \code{dl_lake}. \code{dl_close()} invisibly
returns \code{TRUE}; it is an alias for \code{\link[=dl_disconnect]{dl_disconnect()}}.
}
\description{
Creates or reopens a self-contained local folder. DuckDB is the default and
needs no extension download or external service. The backend is remembered
in \code{lakefold.json}; reopening a folder never silently switches backends.
A new lake needs an empty or nonexistent folder. Existing lakes made with
custom configuration still open through \code{\link[=dl_connect]{dl_connect()}}.
Use \code{\link[=dl_config]{dl_config()}} and \code{\link[=dl_connect]{dl_connect()}} for custom layers or remote storage.
}
\examples{
root <- tempfile("lakefold-")
lake <- dl_open(root)
dl_write(lake, data.frame(id = 1:2), "orders")
dl_read(lake, "orders")
dl_close(lake)
lake <- dl_open(root) # reopens the same data
dl_read(lake, "orders")
dl_close(lake)
unlink(root, recursive = TRUE)
}
\seealso{
\code{\link[=dl_write]{dl_write()}}, \code{\link[=dl_read]{dl_read()}}
}
