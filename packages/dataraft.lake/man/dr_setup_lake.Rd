% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/setup.R
\name{dr_setup_lake}
\alias{dr_setup_lake}
\alias{dr_connect_lake}
\title{Set up or reconnect to a data lake}
\usage{
dr_setup_lake(
  catalog = dr_registry_duckdb("metadata.ducklake"),
  storage = dr_storage_local("data"),
  layers = c("raw", "validated", "products"),
  landing = "landing",
  backend = c("ducklake", "duckdb"),
  install_extensions = TRUE,
  read_only = FALSE,
  path = NULL
)

dr_connect_lake(config, read_only = config$read_only)
}
\arguments{
\item{catalog}{Metadata configuration.}

\item{storage}{Data file storage configuration.}

\item{layers}{Schema names.}

\item{landing}{Local immutable landing directory (also used as staging for
S3).}

\item{backend}{DuckLake, or local DuckDB for offline development.}

\item{install_extensions}{Allow DuckDB to install required extensions.}

\item{read_only}{Attach existing storage read-only and skip schema creation
writes. Only the current registry schema is supported.}

\item{path}{Optional self-contained local folder, as in \code{\link[=dr_open_lake]{dr_open_lake()}}.
Supply this instead of \code{catalog}, \code{storage} and \code{landing}. New folders
default to DuckDB; use \code{backend = "ducklake"} for DuckLake. Saved settings
are reused when reopening.}

\item{config}{A configuration from a previously connected lake.}
}
\value{
A connected lake handle. Close it with dr_disconnect_lake().
}
\description{
Set up or reconnect to a data lake
}
\examples{
\dontshow{if (requireNamespace("duckdb", quietly = TRUE)) (if (getRversion() >= "3.4") withAutoprint else force)(\{ # examplesIf}
root <- tempfile("dataraft-example-")
config <- dr_lake_config(
  dr_registry_duckdb(file.path(root, "lake.db")),
  dr_storage_local(file.path(root, "data")),
  landing = file.path(root, "landing"), backend = "duckdb"
)
lake <- dr_connect_lake(config)
lake
dr_disconnect_lake(lake)
unlink(root, recursive = TRUE)
\dontshow{\}) # examplesIf}
}
\keyword{internal}
